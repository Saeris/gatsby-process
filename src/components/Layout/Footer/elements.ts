import styled, { css } from "styled-components";

export const Container = styled.footer(
  () => css`
    display: flex;
    width: 100%;
    padding: 24px 36px;
    box-sizing: border-box;
  `
);
